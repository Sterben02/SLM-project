services:
  scanner:
    build: .
    image: slm-scanner:latest
    volumes:
      - ./reports:/app/reports
    # По умолчанию: полный каскад с SLM
    command: ["scan", "examples/example_repo", "--with-slm", "--format", "markdown", "--out", "reports/report_docker.md"]
